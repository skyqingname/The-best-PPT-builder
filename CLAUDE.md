# CLAUDE.md

本文件是项目真源。改行为先改这里，再改代码。

## 这是什么

按 Sandun 在 linux.do 公开的 PPT Agent 思路，复刻一条可自动跑完的本地网站。

- npm 包名：`ppt-agent`；不要依赖仓库目录是否包含空格
- 对标产品形态：https://sandun.cc/ 的主链路，不是官方站全功能
- 思路真源：`linux-do-topic-1782304-ai-text-1786933847326.txt`
- 界面壳参考：用户提供的项目工作台截图（左缩略图 / 中画布 / 上「搜索·初稿·设计稿」/ 右过程日志与改稿）
- 开源：做完后再上传。现在不建公开仓库、不 `git push`、不发布

与 SANDUN 官方无关。README 将来必须写清这一点，并链接原帖。

## 优先级

1. 复刻原帖效果和方法
2. 截图里的工作台信息架构
3. 其它材料（包括 zhx47/ppt-agent）只当参考或反例

帖子已经写明的流程、页型和提示词，不要再发明一套。

## 明确不做（v1）

- 登录、云端历史、案例画廊、邀请、计费、旗舰/专家档位
- 把上传的参考 PPT/PDF 直接转换成用户的新 PPT（上传文件只用于提取视觉风格，不复用其事实内容）
- 独立图搜、文生图、卡片贴实拍图
- 页级向量库、PostgreSQL、pgvector
- 把 SVG 拆成 Office 可编辑形状
- 多用户、可部署鉴权
- 复用 ppt-master / open-kimi-ppt / 其它 PPT skill 的方法

## 产品行为

- 本机单用户。打开就能用，无登录。
- 一句话开项目后先自动完成背景调研，再停在「内容需求单」。系统为 3–5 个关键问题给出推荐答案，用户可逐题选择、编辑或直接采用；提交后自动跑完大纲结构板 → 按页检索 → 整套 Deck Plan → 策划稿，然后停在「设计参考确认」。
- 默认有两个产品停顿点：需求确认，以及全部策划稿完成后的设计参考确认。第二个停顿点允许选择内置风格与颜色，或上传 PPT/PPTX/PDF 作为视觉参考；确认后自动完成设计稿与导出准备。
- 所有会触发模型的 HTTP 操作只负责校验、登记与唤醒项目 worker，不在请求生命周期内等待模型完整返回；接口应快速返回当前项目快照，真实进度由项目 SSE 推送。
- 假设（页数、受众、目的、风格与问题答案）在需求单和工作台内可查看。用户后续修改时只重算下游，不整单回滚。
- 大纲一出来先上「空间结构板」：无限浅色点阵画布，可平移、缩放、全局查看和拖动节点；按章节展示大纲、页级资料、内容策划、策划稿和设计稿，并用连线表达产物关系。页面顺序通过专用拖拽手柄调整，画布自由位置只保存在浏览器本地。后台继续按页出稿；用户改某页标题、要点或顺序，只重算对应下游。
- 结构板右侧固定文本模型对话台，作用范围可选整套、当前章节或当前页。模型只生成结构化修改提案，界面先显示 diff，用户明确应用后才修改页面、顺序或章节并触发精确失效；不得收到一句话就直接改库。
- 出稿后的主界面按用户真图：左侧窄缩略图栏、中间 16:9 画布、顶部「搜索 / 初稿 / 设计稿」分段入口、右侧阶段卡片 + 当前页改稿，底部演讲备注。右栏不展示原始事件流水账，而把事件归并成调研、需求确认、结构板、初稿、设计稿等可理解状态。
- 桌面端优先还原真图的紧凑白色工作台、浅灰画布、蓝色主操作与圆角卡片；窄屏时右侧过程栏收拢到主内容下方，不能让画布被挤成不可用尺寸。
- 产品视觉采用 `Blueprint Atelier`：像一间正在运转的数字演示工坊。首页、需求流和结构板可以使用不对称排版、动态网格、指针响应光场与编排式入场；编辑工作台必须保持高密度、低干扰，先锋感来自字阶、空间、材质和状态运动，而不是遮挡画布的装饰。
- 功能图标统一使用 Lucide。界面文案和控件禁止使用 emoji、文字箭头或自行绘制的功能图标；装饰性几何图形不受此限制。动画优先使用 `transform` 与 `opacity`，必须支持 `prefers-reduced-motion`。
- 大纲的 `parts[].part_title` 必须实体化为 `section` 章节分隔页。总页数包含封面、目录、章节页、内容页和结尾。
- 所有页级资料完成后，文本模型只额外调用一次，生成整套 Deck Plan：共享字阶、网格、边距、标题区、卡片几何、基础颜色和图形语言，以及每页的布局类型、信息层级与视觉槽位。Deck Plan 是策划稿跨页一致性的唯一真源。
- 内容页：Bento Grid，布局由内容驱动，先按 Deck Plan 生成无品牌装饰的统一策划稿，再套风格画像出设计稿。需要视觉表达时必须在 Deck Plan 中声明 `diagram / chart / svg_illustration / photo` 槽位及其目的、位置和比例；策划稿可以保留明确的实拍图意图，最终稿不得留下空占位。
- 封面、目录、章节、结尾：专用提示词，不用 Bento；仍然先策划后设计。
- 画布固定：整页 SVG，`viewBox 0 0 1280 720`。
- 配图：模型用 SVG 画图标、色块、几何、文字。没有图就不要硬塞照片。
- 风格：内置 4–6 个风格包（配色、气质、背景、禁忌；不写死版式）。策划稿完成后必须让用户确认。用户可选内置风格和颜色，也可上传一个 `.ppt`、`.pptx` 或 `.pdf` 参考文件；参考文件经本机隔离转换和一次视觉模型分析后产出风格画像，用户确认画像后才允许生成设计稿。上传文件只保存在 `data/artifacts/<projectId>/references/`，不得提交、上传第三方存储或当作内容事实来源。
- 导出：每页一张完整 SVG 嵌进 PPTX，能放映、能带走。不保证 Office 里拆成可编辑形状。

## 资料

- 项目级先检索，用来生成假设和大纲。
- 页级各自搜索 → 优先用搜索模型带回的来源正文/摘要 → 不够再尝试抓 URL → 写成该页 summary。
- 每页一个资料袋，互不混用。
- 不上向量库、不做跨页召回。
- 初始调研只发起一次覆盖多个维度的联网搜索，不先调用文本模型生成一组搜索词再逐条请求。内容页的查询规划与联网搜索合并为一次搜索模型请求，搜索结果在本地组装为带来源标签的证据简报；封面、目录、章节页和结束页直接使用项目资料与大纲，不执行页级搜索。
- 来源正文优先使用搜索模型返回的 `content/snippet`。只有正文不足时才补抓排名靠前的最多两条 URL；相同 URL 在当前进程内按 TTL 复用，禁止每个查询重复串行抓取全部结果。
- 默认调用预算以 12 页、约 7 个内容页为例：初始搜索、需求假设和大纲共不超过 3 次模型请求；整套 Deck Plan 1 次；每个内容页最多一次搜索、一次初稿、一次设计稿；固定页只调用初稿与设计稿。使用内置风格的正常主链路目标约 35 次模型请求；上传参考稿额外增加一次视觉分析，而不是按每页拆出多轮搜索词和摘要调用。
- 搜索不是独立搜索供应商配置，而是第三套完整模型配置：Base URL、API Key、协议、模型名。它与文本模型、SVG 模型使用同一套协议适配和模型列表拉取能力；所选模型必须自身具备联网搜索能力，并按搜索提示词返回可核验的来源 URL。搜索模型使用 Responses 协议时，调用层必须启用 `web_search` 服务端工具并用 JSON Schema 约束来源结构；Grok 联网搜索只允许使用 Responses，不能退回提示词模拟搜索。Responses 联网搜索必须使用 SSE 流式响应，避免长时间工具调用被兼容网关按空闲连接超时；不能依赖 xAI 尚未实际启用的 `background` 参数。流式事件必须归并成“请求已提交 / 模型已响应 / 正在搜索网页 / 正在整理来源 / 搜索完成”等有限进度状态，通过项目 SSE 同步到需求调研和工作台右栏；禁止把每个文本 token 写入事件表。429、模型容量已满、临时不可用和 502/503/504/524 属于可恢复错误，搜索调用按 5、15、30、45 秒退避，最多请求 5 次；等待期间显示下一次重试时间并响应项目暂停。其它错误立即失败。网关返回 HTML 错误页时，界面只展示状态码和净化后的摘要，不回显整页 HTML。

## 技术

- 全新空仓库重写。不要 fork zhx47。
- Next.js 全栈 + SQLite。一个仓库、一次启动。
- 长任务用本进程队列，右侧用 SSE 看过程。
- 同一项目同时只能有一个 worker。运行中收到假设、页面或风格修改时，先标记取消，再由同一 worker 串行消费最新请求；禁止固定延时后另起任务。
- worker 使用 single-flight 语义：同项目重复唤醒只合并为一次后续运行；模型调用必须绑定项目级 `AbortSignal`，暂停或新版本到来时立即中止旧请求，不能等旧调用自然结束后再丢弃结果。
- 页面处理按阶段批量推进：先完成所有待检索内容页，再用一次文本模型调用生成整套 Deck Plan，再完成所有待生成策划稿页，暂停等待设计参考确认，最后完成所有待生成设计稿页。阶段内按页串行，避免高并发触发供应商容量和限流错误。
- 页面失效范围按 `search → plan → draft → design` 表达：改标题、要点从搜索重跑；仅调顺序不重复搜索，但使 Deck Plan 与受影响页面策划/设计稿失效；改策划稿从策划稿重跑；只换风格或下视觉指令只重跑设计稿。
- 进程重启后，访问仍标记为 `running` 的项目会自动恢复；用户也能在工作台暂停和继续。
- 模型不绑品牌。设置里三套完全独立的配置（文本 / SVG / 搜索），各有 Base URL、API Key、协议、模型名。
- 模型网关对上层只暴露结构化 JSON、普通文本、SVG、联网搜索四种语义调用。协议拼装、错误净化、SSE 解码、超时、取消与有限重试不能散落在流水线业务代码中。
- 协议四选一：
  1. OpenAI Responses（`/v1/responses`）
  2. Messages（`/v1/messages`）
  3. Gemini（`/v1beta/models`）
  4. `/v1/chat/completions`
- 交互按 CC Switch：填完地址和 Key 可拉取模型列表并选择；拉不到允许手填。
- Key 只存在本机 SQLite，不进仓库、不进日志、不进提交。
- 大纲结构沿用原帖 JSON：`cover` / `table_of_contents` / `parts[].pages` / `end_page`。
- `parts[].part_title` 在页面层实体化为 `section` 页，但不改变原帖大纲 JSON 契约。
- 内容页 Bento 规则沿用原帖提示词，不要改成固定模板。
- 页面 API 必须同时校验项目 ID 和页面 ID。导出只接受全部 `design_status=ready` 的项目，不得过滤空稿后静默缺页。
- 模型返回的 SVG 必须在服务端从代码块或混杂文本中提取独立根节点，校验 `viewBox="0 0 1280 720"`、`width="1280"`、`height="720"`，并拒绝脚本与外部图片 URL。只有通过 XML 解析、Resvg 实际渲染且画面不是空白的结果才能写入并标记为 `ready`；禁止用字符串包含 `<svg>` 代替可渲染性验证。
- 已存储稿件在项目读取、继续生成和导出前都要执行 SVG 完整性审计：可从模型解释文字中无损提取真实根节点时直接自修复；无法修复时清空坏稿并把对应阶段标记为 `stale`，不得继续显示、放映或导出坏页，也不得仅因历史状态是 `ready` 而跳过重生成。
- PPTX 导出保持每页完整 SVG，并提供 PNG 兼容回退；生成包必须保留原页面顺序、拒绝缺页，并通过 ZIP 结构与 LibreOffice 打开/转换验证。重复下载同一项目版本时允许复用内存缓存，不能重复执行无意义的渲染。
- 项目 SSE 只在真实状态变化时发送项目快照。保活使用不会触发 `message` 的 SSE comment，间隔不短于 15 秒；前端禁止同时开启轮询和 SSE。前端收到密集更新时必须 single-flight 合并刷新或直接消费推送快照。
- Deck Plan、结构对话提案、参考稿与风格画像使用 `data/artifacts/<projectId>/` 下的版本化 sidecar 文件，不修改 SQLite schema。画布缩放与自由节点位置使用 `localStorage`；只有语义顺序和已应用的内容修改写入后端。
- 参考文件上限 50MB，最多分析 40 页并抽样不超过 12 页；校验扩展名与文件签名，拒绝宏文件。Office 文件使用隔离的 LibreOffice 用户目录转换，PDF 使用 Poppler 生成低分辨率参考图；转换必须有超时和能力检查。视觉分析使用已配置且支持图片输入的 SVG 模型，一份参考文件最多一次分析调用。

## 提示词

原帖已公开的两段是正式契约，改之前先改本文件并说明原因：

1. 「顶级的PPT结构架构师」+ `[PPT_OUTLINE]` JSON
2. 内容页 Bento + 整页 SVG（`0 0 1280 720`）

上述两段必须在代码中保留原帖原文核心，不得改写成另一套角色或布局方法。工程约束只能追加在原文之后，并且不能与原文冲突。原帖没有公开设计稿阶段的正式 Prompt；本项目的设计稿提示词只能落实原帖明确描述的“策划固定内容与版式，设计增加风格效果”，不能伪称或臆造原帖 Prompt。

搜索模型提示词：必须要求模型实际调用可用的联网能力，只返回 `results[]` JSON；每条包含标题、公开网页 URL、摘要和来源事实片段。无可靠结果时返回空数组，禁止补造链接。调用层仍须用结构化输出约束，不能把提示词当作格式保证。
大纲提示词：原帖核心之后追加内容质量底线。内容页标题必须结论先行，`content` 必须是可上屏的具体短句；禁止“待补充 / 待核实 / 相关情况 / 提供服务 / 展示实力”等占位式、套话式大纲。公开资料不足时缩小论证范围，不能用占位文案假装完成。
Deck Plan 提示词：一次输出整套共享视觉合同和逐页计划。共享合同必须足够具体，使不同页面调用仍保持同一套字阶、边距、标题结构、卡片几何和基础图形语言；逐页计划必须给出页面目标、布局组合、信息层级、视觉槽位和阅读顺序，不得生成 SVG。
策划稿提示词：内容页使用原帖 Bento 原文并接收 Deck Plan；封面、目录、章节、结尾使用各自独立提示词，系统消息里不得出现 Bento 再用一句“不要 Bento”互相抵消。策划稿要像原帖示例一样把真实文案、信息层级、卡片几何、图表/流程/配图区域和版式完整定好，清爽但不是空线框。可以对搜索事实做忠实压缩，不得增加输入中不存在的年份、英文眉题、保密标签、署名、版本号和企业宣传套话。
设计稿提示词：内容页必须包含原帖 Bento 原文，并同时接收策划稿、Deck Plan、风格画像与页面视觉槽位；封面、目录、章节和结尾使用专用设计规则。不许改变事实、阅读顺序、主要信息分区和卡片外部边界；允许在分区内部做必要的光学校正、字号微调和视觉重构，必须增加完整风格、图形细节与数据表达，不能只做换色。策划稿中的图表、流程、关系或主题视觉区域必须做成真实 SVG 信息图；实拍图槽位没有可用内容素材时改成完整 SVG 主题视觉，不能保留空框。
SVG 输出约束：只使用可被 Office 稳定渲染的 SVG 元素，不使用 `foreignObject`、脚本或外部图片；第一个非空字符必须是 `<svg`，最后一个非空字符必须是 `</svg>`，不得在 SVG 前后输出分析、解释或 Markdown 围栏。禁止输出“待补充 / 待核实 / 占位 / 示例区域”等制作过程文案。封面只使用输入给出的标题、副标题和必要点题文案，不得自行生成 `PRESENTED BY`、`EDITION`、`CONFIDENTIAL` 等模板元数据。

## 界面复刻契约

- 需求确认：对话式纵向信息流；顶部保留用户原始主题气泡；展示背景调研完成态、来源摘要和「内容需求单」进度；问题用 A–D 选项胶囊呈现，推荐项默认选中，支持自定义答案；最后统一提交。
- 结构板：采用 `Spatial Editorial Atelier` 方向。无限点阵画布上按 `cover / toc / section / content / end` 形成可扫读故事线，并横向展开搜索资料、内容策划、策划稿和设计稿；章节容器、细蓝连线、缩放感知的信息密度构成主要视觉记忆点。页面卡展示页码、标题、摘要和搜索/策划/初稿/设计状态，选中态只使用蓝色描边与浅蓝底，不使用多色随机纸张或通用流程图库皮肤。
- 结构板右侧是 `Script Desk` 对话台：顶部选择修改范围，中部展示真实对话与待应用 diff，底部输入框固定；任何会删除页面或重排结构的提案必须由用户点击应用。
- 编辑工作台：左栏宽度约 190–220px，右栏约 360–440px，中间画布保持最大可用面积；缩略图选中态使用蓝色描边；顶部主操作仅保留放映、导出和必要的运行控制；右栏底部改稿框始终针对当前页和当前 surface。
- 搜索、初稿、设计稿是同一页的三种 surface。切换不能改变当前页，也不能用设计稿替代尚未生成的设计结果而不提示状态。
- 视觉风格采用克制的产品化蓝白体系，重点是层级、密度、对齐和状态表达，不照搬对标产品的商标、名称或专有文案。

## 验证

改完必须真跑，不要只改不验。

- 类型检查：`npx tsc --noEmit`（或项目里等价命令）
- 回归测试：`npm test`（测试不得访问真实模型、搜索服务或用户数据库）
- 生产构建：`npm run build`
- 主链路至少手跑一套：一句话 → 内容需求单 → 结构板 → 至少 1 个内容页的策划稿 SVG + 设计稿 SVG → 导出 PPTX
- SVG 必须能在浏览器里打开，画布是 1280×720，文字可读、卡片不乱叠
- 改假设 / 改结构板单页后，只重算下游，其它已完成页不被删除
- 设置里文本、SVG、搜索三套模型能分别保存和拉取模型列表；搜索模型配置完成后能直接用于调研

## 协作

- 默认中文沟通；代码、命令、变量名用英文
- 密钥、token 不进代码、不进 commit、不进日志
- 删除文件、改密钥相关配置、对外发布，先问
- 不要用其它 PPT skill 的坐标协议、PPTD、语义页协议来污染本项目
