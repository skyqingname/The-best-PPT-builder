# CLAUDE.md

本文件是项目真源。改行为先改这里，再改代码。

## 这是什么

按 Sandun 在 linux.do 公开的 PPT Agent 思路，复刻一条可自动跑完的本地网站。

- 工作目录名：`ppt-agent`
- 对标产品形态：https://sandun.cc/ 的主链路，不是官方站全功能
- 思路真源：`linux-do-topic-1782304-ai-text-1786933847326.txt`
- 界面壳参考：用户提供的项目工作台截图（左缩略图 / 中画布 / 上「搜索·初稿·设计稿」/ 右过程日志与改稿）
- 开源：做完后再上传。现在不建公开仓库、不 `git push`、不发布

与 SANDUN 官方无关。README 将来必须写清这一点，并链接原帖。

## 优先级

1. 复刻原帖效果和方法
2. 截图里的工作台信息架构
3. 其它材料（包括 zhx47/ppt-agent）只当参考或反例

帖子已经写明的流程、页型和提示词，不要再发明一套。

## 明确不做（v1）

- 登录、云端历史、案例画廊、邀请、计费、旗舰/专家档位
- 上传 PPT 美化、PDF 转 PPT
- 独立图搜、文生图、卡片贴实拍图
- 页级向量库、PostgreSQL、pgvector
- 把 SVG 拆成 Office 可编辑形状
- 多用户、可部署鉴权
- 复用 ppt-master / open-kimi-ppt / 其它 PPT skill 的方法

## 产品行为

- 本机单用户。打开就能用，无登录。
- 一句话开项目后自动跑完全程：需求调研 → 提问（写成可改假设并自动作答）→ 大纲便利贴 → 按页检索 → 策划稿 → 设计稿 → 可导出。
- 阶段可见，可打断。不改就不停。不是每步都要点一次。
- 假设（页数、受众、目的、风格等）展示出来。用户改了只重算下游，不整单回滚。
- 大纲一出来先上便利贴墙；后台继续按页出稿。用户改某张贴的标题/顺序，只重算该页下游。
- 出稿后的主界面按截图：左缩略图、中画布、上「搜索 / 初稿 / 设计稿」、右过程日志 + 当前页改稿、放映、导出。底部可留演讲备注输入，不作为主链路门槛。
- 内容页：Bento Grid，布局由内容驱动，先无风格策划稿，再套风格包出设计稿。
- 封面、目录、结尾：专用提示词，不用 Bento；仍然先策划后设计。
- 画布固定：整页 SVG，`viewBox 0 0 1280 720`。
- 配图：模型用 SVG 画图标、色块、几何、文字。没有图就不要硬塞照片。
- 风格：内置 4–6 个风格包（配色、气质、背景、禁忌；不写死版式）。系统按主题写入假设，用户可改；改了只重跑设计稿。
- 导出：每页一张完整 SVG 嵌进 PPTX，能放映、能带走。不保证 Office 里拆成可编辑形状。

## 资料

- 项目级先检索，用来生成假设和大纲。
- 页级各自搜索 → 优先用搜索接口带回的正文/摘要 → 不够再尝试抓 URL → 写成该页 summary。
- 每页一个资料袋，互不混用。
- 不上向量库、不做跨页召回。
- 搜索在设置里选预设供应商（先做 Tavily、博查），只填一把 Key。

## 技术

- 全新空仓库重写。不要 fork zhx47。
- Next.js 全栈 + SQLite。一个仓库、一次启动。
- 长任务用本进程队列，右侧用 SSE 看过程。
- 模型不绑品牌。设置里两套完全独立的配置（文本 / SVG），各有 Base URL、API Key、协议、模型名。
- 协议四选一：
  1. OpenAI Responses（`/v1/responses`）
  2. Messages（`/v1/messages`）
  3. Gemini（`/v1beta/models`）
  4. `/v1/chat/completions`
- 交互按 CC Switch：填完地址和 Key 可拉取模型列表并选择；拉不到允许手填。
- Key 只存在本机 SQLite，不进仓库、不进日志、不进提交。
- 大纲结构沿用原帖 JSON：`cover` / `table_of_contents` / `parts[].pages` / `end_page`。
- 内容页 Bento 规则沿用原帖提示词，不要改成固定模板。

## 提示词

原帖已公开的两段是正式契约，改之前先改本文件并说明原因：

1. 「顶级的PPT结构架构师」+ `[PPT_OUTLINE]` JSON
2. 内容页 Bento + 整页 SVG（`0 0 1280 720`）

策划稿提示词：只定信息层级和版面，禁止注入最终风格。
设计稿提示词：不许改文案、卡片结构、阅读顺序，只上风格包。

## 验证

改完必须真跑，不要只改不验。

- 类型检查：`npx tsc --noEmit`（或项目里等价命令）
- 主链路至少手跑一套：一句话 → 便利贴 → 至少 1 个内容页的策划稿 SVG + 设计稿 SVG → 导出 PPTX
- SVG 必须能在浏览器里打开，画布是 1280×720，文字可读、卡片不乱叠
- 改假设 / 改单页便利贴后，只重算下游，其它已完成页不被删除
- 设置里两套模型和一把搜索 Key 能分别保存、拉取模型、切换后生效

## 协作

- 默认中文沟通；代码、命令、变量名用英文
- 密钥、token 不进代码、不进 commit、不进日志
- 删除文件、改密钥相关配置、对外发布，先问
- 不要用其它 PPT skill 的坐标协议、PPTD、语义页协议来污染本项目
